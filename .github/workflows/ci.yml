name: CI

on:
  push:
    branches: [main, master]
    paths:
      - "VaultCloud/VaultCloud/**"
      - ".github/workflows/**"
  pull_request:
    branches: [main, master]
    paths:
      - "VaultCloud/VaultCloud/**"
      - ".github/workflows/**"

jobs:
  backend:
    name: Backend Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: VaultCloud/VaultCloud/backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: VaultCloud/VaultCloud/backend/package-lock.json

      - name: Install backend dependencies
        run: npm ci

      - name: Validate backend syntax
        run: node --check src/server.js

  frontend:
    name: Frontend Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: VaultCloud/VaultCloud/frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: VaultCloud/VaultCloud/frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build
